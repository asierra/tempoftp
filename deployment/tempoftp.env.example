# Variables de entorno para tempoftp (copiar a /etc/default/tempoftp o /etc/sysconfig/tempoftp)
# Ruta de trabajo del proyecto (debe coincidir con WorkingDirectory del servicio)
WORKDIR=/opt/tempoftp

# Puerto de la API
PORT=9043

# Modo real
TEMPOFTP_SIMULACRO=0

# Base de datos de pure-ftpd
FTP_DB_HOST=localhost
FTP_DB_PORT=3306
FTP_DB_USER=pureftpd_user
FTP_DB_PASS=secret
FTP_DB_NAME=pureftpd_db

# Usuario SSH para du -sb y rsync (si no se define, se usa lanotadm)
# RSYNC_SSH_USER=lanotadm

# Si usas venv, agrega la ruta al PATH aquí
# PATH=/opt/tempoftp/.venv/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin
